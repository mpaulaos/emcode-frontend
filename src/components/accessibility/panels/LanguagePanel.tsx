export function LanguagePanel() {
  return (
    <fieldset>
      <legend className="mb-3 text-sm font-semibold text-text-headings">Idioma</legend>
      <p className="rounded-lg border border-border-card px-3 py-2 text-sm text-text-body">
        Idioma actual: <span className="font-medium text-text-headings">Español (es)</span>
      </p>
    </fieldset>
  );
}
